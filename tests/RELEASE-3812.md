# Validation

70 page-deck/controller checks pass. 174 release checks pass using a simulated DOM: splash readiness, timeout and tap dismissal; idempotence; saved font migrations; all cache paths; new font wiring; and license dialog loading/closing. JavaScript syntax checks pass.

Local browser preview was blocked by environment policy. These checks do not validate Android rendering. On a phone, inspect the first launch splash, shelf/book alignment, theme variants, font selections, offline license dialog, and retained forward/back page motion. Preview 3812 is a separate install.
